#!/bin/bash
#
# Optimized environment configuration for Ethereum/Optimism client builds.
# This configuration primarily uses version tags for stability and simplifies 
# variable naming by removing redundant commit hashes.

# --- Client Configuration Blocks ---

## BASE Reth Node (Execution Layer Client for Base)
export BASE_RETH_NODE_REPO="https://github.com/base/node-reth.git"
# Using TAG for version control for reliability and ease of updates.
export BASE_RETH_NODE_TAG="v0.2.1"

## NETHERMIND (Execution Layer Client)
export NETHERMIND_REPO="https://github.com/NethermindEth/nethermind.git"
export NETHERMIND_TAG="1.35.2"

## OP-GETH (Optimism-forked Geth - Execution Layer)
export OP_GETH_REPO="https://github.com/ethereum-optimism/op-geth.git"
export OP_GETH_TAG="v1.101603.5"

## OP-NODE (Optimism Node - Consensus/Sequencing Layer)
export OP_NODE_REPO="https://github.com/ethereum-optimism/optimism.git"
export OP_NODE_TAG="op-node/v1.16.2"

## OP-RETH (Optimism-forked Reth - Execution Layer)
# Note: This is linked to Paradigm's main Reth repository, indicating a specific branch/tag 
# related to OP development within the standard Reth repo.
export OP_RETH_REPO="https://github.com/paradigmxyz/reth.git"
export OP_RETH_TAG="v1.9.2"

# --- Redundant variables removed for cleanliness (e.g., *_COMMIT). ---
# If a specific commit hash (not linked to a tag) is required for patching 
# or specific testing, the *_TAG variable should be replaced with *_COMMIT.
